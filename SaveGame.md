[[SaveGameSubsystem]]


I want to make a note taking app with unreal , it will have a custom actor with a widget component, which will have a editable single line widget for a heading and  a editable multiline widget for the matter of the note


I want the notes to be saved Asyncly after every 1 min, but I want an array of the custom actor notes that have been changed till the last save so that it only saves the changed actors , make a saveGameGameInstanceSubsystem, and an interface which is implemented by the custom note actors
