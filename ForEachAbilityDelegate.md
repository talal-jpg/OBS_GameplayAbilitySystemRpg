![[forEachAbility]]

![[broadcastAbilityInfoForEachDelegate]]

Delegate can be of two type Dynamic and non Dynamic

// says the same thing in different order
non Dynamic ones can bind a lambda func and can execute that func with Delegate.Execute


In Broadcast Ability Info func the lambda functionality is being bound to, and is being called from ASC
Only using .Execute instead of .Broadcast  , this confused me.. 